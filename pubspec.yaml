name: travel_organizer
description: "Travel Organizer Offline - App per organizzare viaggi senza connessione"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  sqflite: ^2.3.3+1
  sqflite_common_ffi_web: ^0.4.5+2
  path: ^1.9.0
  uuid: ^4.4.0
  intl: ^0.19.0
  fl_chart: ^0.70.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
